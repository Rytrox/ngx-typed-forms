import { InputTextFormDirective } from './input-form.directive';

describe('InputFormDirective', () => {
  it('should create an instance', () => {
    const directive = new InputTextFormDirective();
    expect(directive).toBeTruthy();
  });
});
