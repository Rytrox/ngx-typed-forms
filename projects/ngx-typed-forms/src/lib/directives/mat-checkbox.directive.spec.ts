import { MatCheckboxDirective } from './mat-checkbox.directive';

describe('MatCheckboxDirective', () => {
  it('should create an instance', () => {
    const directive = new MatCheckboxDirective();
    expect(directive).toBeTruthy();
  });
});
